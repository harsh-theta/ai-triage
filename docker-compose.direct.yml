services:
  backend:
    environment:
      # Override: direct access without base path
      - ROOT_PATH=

  frontend:
    environment:
      # Override: direct access without base path
      - NEXT_PUBLIC_BASE_PATH=
      - ROOT_PATH=